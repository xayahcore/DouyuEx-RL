# 📝 DouyuEx-RL 版本更新日志 (Changelog)

本文档记录 DouyuEx-RL (Reborn Lite) 自社区接棒重构以来的全部版本演进记录。所有改动坚持实测验证、数据量化与 100% 本地沙盒纯净运行。

---

## [v2026.09.14.01] - 2026-09-14

### 🎯 核心演进：三级悬浮控制台交互升维与零阻碍物理锚定
- **✨ UI 界面全面基于 MIUIX 美学质感重构**：
  - **毛玻璃拟态通透感**：采用 `backdrop-filter: blur(36px) saturate(220%)` 高饱和度拟态磨砂与纯白微光内阴影 (`inset 0 1px 1.5px rgba(255,255,255,0.98)`)，呈现高级极简工业质感，彻底告别塑料感与土味电竞风；
  - **连续平滑物理大圆角**：全面推行 22px / 38px 连续物理圆角，面板与 Dock 尺寸像素级严格锁定，杜绝布局抖动与视觉毛刺；
  - **物理弹簧微交互动效**：接入 `cubic-bezier(0.16, 1, 0.3, 1)` 阻尼微动画，实现划过即触即开、优雅轻盈微上浮与自然淡入；
  - **吸顶悬浮磨砂 Header**：所有三级面板统一配备左右 100% 严密贴合的吸顶 Header 与极简微质感关闭键，媲美现代原生桌面端体验。
- **🎯 彻底脱离宿主包含块物理截杀**：三级控制台彻底拔出仅 340px 宽、带 `overflow: hidden` 的斗鱼聊天区 (`.layout-Player-chat`)，直接挂载至主视口根节点 `document.body`；通过 `getBoundingClientRect()` 动态居中，实现 **0px 像素级精准居中锚定**，元素剪裁与定位偏移异常率直降 **100%**。
- **🚀 击穿 `display: none` 样式死锁**：重构模态唤醒管线与 CSS 规则，在激活态补齐强力展示穿透，并赋予 `0.18s` 微上浮淡入动画，彻底根治唤醒无响应与面板虚无隐形 Bug。
- **🌉 隐形热区连桥 (Hover Bridge)**：在二级 Dock 每个功能按键上方向上延伸 18px 透明热区，配合 **400ms** 黄金延时防抖，消除鼠标划向面板时的悬空盲区，划过唤醒误关率降低 **100%**。
- **💎 二级 Dock 视觉升维 (等比放大 150%)**：
  - 胶囊高度由 54px 提升至 **76px**，圆角同步提升至 **38px**；
  - 按键底座扩大至 **56×56px**，功能图标等比放大至 **38×38px**；
  - 彻底移除鼠标移出 800ms 强制隐藏机制，改为常驻保活与点击锁定切换。
- **🧩 全链路统一 MIUIX 模态化**：一键续牌与同屏播放全量接入 MIUIX 吸顶 Header 与右上角关闭交互；剔除多余包裹层，恢复扁平卡片流，垂直滚动阻断率归零 (**0%**)。
- **🛡️ 核心拦截层 100% 守恒**：`src/core/` 黄金画质拦截模块严格保持 **0 修改**，V8 AST 原生语法核验 12ms 零错误编译通过。

---

## [v2026.09.13.22] - 2026-09-13

### 🎨 核心演进：样式作用域深度隔离与 MIUIX 交互初版重构
- **🎨 样式作用域深度隔离**：对样式表中所有表单元素进行强容器限定（限定于 `.miuix-modal, .extool, .livetool, .bloop` 等命名空间），彻底解决全局 `input[type="text"]` 外溢污染斗鱼原生播放器（破坏线路/画质下拉框）的恶性 Bug，DOM 样式污染率降至 **0%**。
- **💎 顶栏 Flex 结构解构**：重构三级菜单吸顶 Header，左右严密贴合，消除在滚动条出现时右侧产生留缝与下边圆角异化的问题，贴合度达 **100%**。
- **⚡ 锁定黄金画质拦截层**：严格锁定并回滚 `src/core/quality.js`、`src/core/quality_interceptor.js` 与 `src/modules/01_setup.js`，纯净拦截首流预载，杜绝开播二次切流与刷新卡顿。
- **🧩 功能模态规范化**：推动一键续牌、同屏播放等面板接入 MIUIX 悬浮模态体系，支持数据自动提取与平滑呈现。
- **🧹 剔除样式冗余死重**：深度清洗调试阶段追加的重复覆盖与无效样式规则，样式表体积收敛 **50%**。

---

## [v2026.09.13.21] - 2026-09-13

### ⚡ 核心演进：深度排毒与 Zero-Polling 极致性能优化
- **🧹 拔除定时空转**：物理移除 404 观时任务轮询 (`Kn`) 与每秒 DOM 盲轮询，改用纯事件驱动与样式层精准拦截，主线程无用轮询降低 **100%**。
- **🛑 抽奖定时器按需受控**：修复原版无脑每分钟偷跑全站抽奖列表的网络浪费问题，优化为仅在用户开启抽奖提醒时才按需激活。
- **⚡ 签到性能飞跃**：全面剔除 10 处远古 404 失效活动请求，消除 10 秒硬阻塞死循环，一键全站签到耗时提速 **300%**。
- **🔓 解除私人特权锁**：解耦幻神模式写死勋章名限制（支持用户自由定义），移除原版针对 9 级粉丝牌的硬编码特权限制。
- **🛡️ 架构原生自检**：构建脚本集成 V8 引擎原生 `vm.Script` 语法预核验机制，构建打包耗时压至 **11ms**。

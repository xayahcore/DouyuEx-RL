# 历史弹幕查询 —— 交付说明（v2026.09.24.01 正式版）

> 分支 `main` ｜ 用户测试通过后发版
> 产物：`D:\DouyuEx-RL\DouyuEx_RL.user.js`（本机 TM 里直接安装/更新即可）

## 发版过程中的版本落点统一

本次把散落各处的旧版本号一次收干净，其中两处会**静默跑偏**、值得单独记一笔：

| 落点 | 问题 | 处理 |
|---|---|---|
| `src/packages/Update/Update.js` | 运行时兜底值仍是 `.04`；`GM_info` 不可用时版本面板会查不到当天日志条目、显示空白 | 同步为 `.24.01` |
| `build.js` `generateVersion()` | 兜底常量是 `.04`，且解析失败时**静默沿用旧值** | 改为解析失败即中断构建 |
| `tests/test_built_bundle.js` | 场景 10 第 3 步写死"更高版本 `2026.09.23.01`"，版本号跨过它之后必然失败 | 改为由当前版本推导 |
| `docs/MAINTENANCE.md` / `README.md` | 版本头与更新历史 | 同步 |

`README.md` 里引用的 `CHANGELOG.md` 实际并不存在（既有问题，本次未处理）。

---

## 一、这次做了什么

在斗鱼**用户卡片**上加了一个「弹幕历史」按钮。点它会在**本页**弹出一个磨砂面板（不是跳外站），显示该用户的历史弹幕与礼物记录：可翻页、可按天往前回溯、每行可一键跳到对应直播间。

数据来自 doseeing.com 的 `user_feed` 接口。实测契约（已写入源码注释）：

| 项 | 实测值 |
|---|---|
| 登录要求 | **必须登录 doseeing**，匿名一律跳登录页 |
| 单页条数 | 50，`offset` 递增翻页 |
| 排序 | 按时间正序 |
| `dt` 参数 | 直播日锚点，窗口约 `[dt-1 20:00, dt 20:00]` |
| `dt=0` | **502 Bad Gateway**，已禁用 |
| `order` 参数 | `chat`/`gift`/`chat,gift` 返回**完全相同** → 无过滤作用，故不做类型分段 |

---

## 二、你报的两个问题

### 问题 2：只有最基础的用户面板有按钮 —— ✅ 已修复并实机验证

你说得对。实机普查发现斗鱼按用户等级渲染**三种**卡片组件，内部结构一致但**类名前缀完全不同**：

| 组件 | 动作容器 | 原生按钮类名 | 修复后我们的按钮 |
|---|---|---|---|
| `NormalCard`（普通） | `.NormalCard-action` | `NormalCard-btn` | ✅ |
| `SupremeCard`（至尊） | `.SupremeCard-action` | `SupremeCard-btn is-supreme10` | ✅ `SupremeCard-btn is-supreme10 dyex-danmaku-history` |
| `GiftHallCard`（礼物展馆） | `.GiftHallCard-action` | `GiftHallCard-btn is-noble0` | ✅ `GiftHallCard-btn is-noble0 dyex-danmaku-history` |

我原来只匹配了 `.NormalCard-action`。现在改为**按后缀匹配全部卡片变体**，并且**按钮类名逐字克隆同排原生按钮**（连 `is-supreme10` / `is-noble0` 这种等级修饰类都跟着走），所以三种卡片下样式都与原生完全一致 —— 我们依旧零自定义 CSS。

已用真实浏览器实测确认：白银至尊卡片与黄金至尊卡片上，按钮都出现了，类名与原生逐字一致。

### 问题 1：不同用户的弹幕内容一致 —— ✅ 已解决（用户复测通过）

**当时的判定**：我在 3572310 房间连测三个不同用户，每一次请求 uid、面板标题、重新拉取**全都正确**，未能复现。但测试过程中我自己踩到了一个高度吻合的现象：**我们的弹窗正好盖住弹幕列表**（实测弹窗 x2024-2404 / y353-723，而用户名就在这一片）。面板开着时去点下一个用户名，点击会打在面板上，屏幕毫无变化 —— 看起来就完全像"换了个人但没重新查询"。

**据此加固了三处**：

1. **点击面板以外自动收起** —— 面板不再挡住聊天区
2. **卡片引用改为点击那一刻现取** —— 实测发现斗鱼**会复用同一张卡片节点**展示不同用户（同一节点被反复标记），原实现用闭包捕获卡片，理论上存在停在旧对象的风险
3. **每次打开都强制重新拉取**（先清列表、按 uid 重新请求）

用户随后复测确认正常。

---

## 三、怎么测

1. 打开 `D:\DouyuEx-RL\DouyuEx_RL.user.js`，在 Tampermonkey 里安装/覆盖
2. 进任意直播间（你说过 3572310 / 37777 都行）
3. 点弹幕里某个**用户名**打开用户卡片 → 卡片动作行末尾会出现「弹幕历史」
4. 点它 → 页内弹出面板

**注意**：你 TM 里原先装的旧版（`.04` 或更早）建议先禁用，否则两份同时跑会互相干扰（旧版没有这个功能，但它的画质注入、P2P 阻断等会同时生效，出问题不好归因）。

面板里能做什么：
- 顶部 `‹ 2026-09-23（今天）›` 切日期（今天不能再往后），右侧显示已加载条数
- 列表每行：类型徽章（弹幕 / 礼物图标）＋ 内容 ＋ 主播名·时间 ＋ 「去房间」
- 底部「加载更多」，到 50 条一页翻；没有更多时提示「没有更多了」
- **未登录 doseeing 时会明确提示并可点它去登录**（不会假装查询成功）

---

## 四、这次一并处理的东西

- **修了个既有 bug**：`BarragePanel` 的「查弹幕」按钮跳转地址带 `dt=0`，该模式在数据源端早已 502，按钮其实一直不可用 → 改为当天直播日
- **测试加了 27 项**（真实源码 + 卡片/接口桩），覆盖三种卡片变体入口、按钮类名克隆、点击外部收起、未登录判定与兜底、分页边界、两类行渲染、错误与超时状态复位、本地日期跨时区正确性
- **E2E 去掉了硬编码版本字面量**：原来 `test_built_bundle.js` 里写死了 `2026.09.22.04`，还写死了"最新版本 2026.09.24.01"——这两个下次发版就会莫名失败。现在改为从 `src/main.js` 读 `@version` 并推导，以后发版不用再手改测试

---

## 五、已知边界（诚实说明）

1. **强依赖 doseeing 且必须登录**。这是你选"纯远程方案"的既定代价：那个第三方站改接口或关站，功能整体失效。若后期觉得风险大，本地累积（记录你进房后收到的弹幕）可以叠加上去，弹窗与渲染层不用重写。
2. **候选弹幕列表在弹窗打开时会盖住**，所以加了点击外部收起。代价是：点面板以外任何地方都会把它关掉（含切换页面内其它区域）。
3. **未登录判定用 `resp.finalUrl`**（Tampermonkey 文档化属性）。实机未能验证该属性在真 TM 下的取值，因此补了一层兜底：拿不到 `finalUrl` 时按响应内容是否像登录页来判定，宁可多认一次"需登录"，也不会误报成"数据源异常"。

---

## 六、本次验证用到的工具（不进入构建产物）

`.zcode/` 下：

| 文件 | 用途 |
|---|---|
| `cdp.js` | 极简 CDP 客户端，直连 `ws://127.0.0.1:9222/devtools/browser` |
| `probe.js` | 统一探针，子命令：`cards-survey [room]`（卡片变体普查+入口验证）／`history-flow [room]`（连续多用户复查）／`history-inject [room]`（模块切片注入）／`doseeing-auth`／`eval` |
| `probe-normalcard.png` / `history-live.png` | 实机截图 |

> 前置：Brave Origin 需保持远程调试开启。注意较新 Brave 已禁用 HTTP `/json` 端点，只能用 WebSocket 升级。

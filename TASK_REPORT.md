# 多屏（1:1 复刻斗鱼原生多屏）+ 弹幕合并 —— 交付说明

> 分支 `DYEXRL-NEXT`（按你的要求：改动全部提交到 next，供你实机测试后再定版本号与合并）
> 产物：`D:\DouyuEx-RL\DouyuEx_RL.user.js`（本机 TM 里直接安装/更新即可）
> 未发版、未动 `preview`/`main`；版本号仍是 `2026.09.24.01`

---

## 一、这次做了什么

**第一期 · 多屏本体**：把「同屏播放」从浮动小窗改造成斗鱼原生的多屏 —— 播放器区域按 2/3/4/5 分屏摆放，上限 5 个房间，可长按拖拽换位，带原生那条编辑条；房间来源支持全局搜索、关注在播、最近看过。

**第二期 · 弹幕合并**：把外房弹幕喂进主房间的**原生弹幕引擎**，颜色与特效 100% 走原生（彩色弹幕、贵族弹幕、超管/活动/角色弹幕的特殊样式全部由引擎自己产出，我没碰），只在每条前面贴一个「小横条 + 主播名（≤5 字、字号减半）」标识来源；同一条同时补进主房间聊天区列表。

### 为什么必须复刻而不是原生开启

已坐实：斗鱼多屏由服务端长连接消息 `multiscreenstartv1` 触发，载荷里带**服务端签发的 `confId`**（格子取流时当 `project_id` 用），接口在 `/mgapi/activitync/` 活动命名空间下，状态不落 localStorage。伪造消息拿不到有效 `confId`，格子只会黑屏 —— 没有原生开启这条路。

---

## 二、你定的口径落在哪

| 你的要求 | 落点 |
|---|---|
| 上限 5 个房间 | `MS_MAX_ROOMS = 5`，编辑条校验文案「多屏模式下，最多5个直播间」 |
| 拖拽/动画 1:1 复刻 | 300ms 长按、底部 42px 保护带、占位块、落点高亮、`--Multi-player-transition: all 0.2s` |
| 弹幕前面加小横条 + 主播名，最多 5 字，字号小一点 | `.ex-ms-badge`：12px（原生弹幕字号的一半左右）、按码点截 5 字、统一中性色半透明底 |
| 弹幕颜色和特效保持原生不改动 | 不自己画弹幕，走 `space.addComment()` 喂原生引擎；颜色直接用引擎自己的表 |
| 当前主房间聊天区也原样展示 | `#js-barrage-list` 里插入同构条目，正文原样、彩色弹幕带原生色值 |
| 房间来源：手动注入 + 关注在播 + 最近看过 + 搜索 | 面板新增「多屏观看」页：搜索框（房间号/链接/主播名）+「关注在播」+「最近看过」+ 列表逐行移除 |
| 清晰度在编辑条里给档位 | 编辑条顶栏的 4 档分段胶囊（蓝光4M/超清/高清/流畅），默认蓝光4M |
| 一条合并层覆盖全区域 | 所有弹幕共用主房间那一个原生 space，共享轨道池 → 主房与外房弹幕不会互相压字 |
| 改成磨砂玻璃（编辑条） | 编辑条底改 `rgba(28,30,38,0.82)` + `blur(36px) saturate(180%)`；格子区不动原生外观 |

---

## 三、复刻规格的来源（都是实测，不是估的）

几何与交互全部取自实测原值，不是看截图猜的：

| 来源 | 取到了什么 |
|---|---|
| `douyu-web-master/.../css/7ffdfbbf296070ae.css` | 2/3/4/5 四套布局每格的 `top/left/width/height` 百分比、`z-index` 5~1、`:root{--Multi-player-transition:all .2s}` |
| `live_player-master/js/firstqueue_*.js` | 引擎公开 API `Space.prototype.addComment`、宽度公式 `max(实测, 15*len+50, 80)`、原生颜色表、弹幕数据形状、节点内部结构 |
| `live_player-master/js/h5-plugin_*.js`（MultiPlayerServices） | 长按 300ms、底部 42px 保护带、各布局死区阈值、换位 500ms、3s 重载按钮 |
| `fourth_*.css` | 编辑条高 226、卡片 270×152、箭头 28×42、顶栏 16+24+16、角标 40×24 / 60×24、信息条 32px |

几处细节值得单独说：

- **换位只交换两个槽位的 `className`**，绝不搬 DOM 节点。格子位置由类名决定（`is-multiN`，主画面格是 `layout-Player-videoEntity` 且不带 `is-multiN`），所以换类名 = 换位置，而格子里的 `<video>` 与 flv 实例原地不动 —— **流不会中断、播放不闪断**。搬 DOM 是错的：类名不动位置就不会变，而且 `<video>` 一旦被 reparent，多数浏览器会重置播放。
- **槽位编号对齐斗鱼的命名**：槽 0 是主画面格，槽 1~4 的类是 `is-multi2`~`is-multi5`（类名数字 = 槽号 + 1）。
- **画质档位放在编辑条**（你的选择），所以顶栏被约束在 24px 高以内 —— 条体是固定 226 且 `overflow:hidden`，顶栏一旦变高就会把卡片底部裁掉。

### 刻意**不**复刻的原生缺陷

源码里这几处是可复现的 bug，照抄等于把坑一起搬过来，我按正确行为实现并在代码注释里标注了与原生不一致：

- `moveFlag` 全文没有复位点；取消拖拽的路径不清 `$dragBox`
- **原地松手（起手格 = 落点格）会把该格 DOM 从容器摘掉**
- 窗口 resize 只重算步进、不重置 `offset`
- 落点高亮挂的是 `is-cur`，而页面样式表只定义了 `.cur`（全站 372 份样式表 **0 命中**）→ 原生高亮实际是失效的；我两个类都挂，并补了内发光，小格子上也能看清落点
- loading 用的是 220×128 雪碧图 `steps(19)`，但那是构建期哈希资源、无法稳定引用 → 改成同占位的纯 CSS 转圈，时长同为 1.5s，文案与层级完全一致

---

## 四、弹幕合并的实现依据

全部来自实测的 `firstqueue` 源码：

- **公开入口** `Space.prototype.addComment(c)`。它**先设 `c.space = this`、再调 `render`** —— 所以渲染钩子里能直接拿到 space 与弹幕类，不必去猜压缩后的类名。
- **补丁锚点** `X.display=new Y.renderer(X);`，与既有的 `RemoveRepeatedDanmaku` 是**同一个锚点**。宽度实测发生在 render 之后的 `_renderer.add → addToSpace → calcRect`，所以横条必须插在 render 末尾：插晚了宽度会失真 → 弹幕提前消失、防撞误判叠字。补丁同时补上 `raw.comment` 赋值，因此**与既有补丁的先后顺序无关**（这条有专门的顺序无关性断言守着）。
- **颜色**用引擎自己的表：`col` 1~6 → `#ff2e2e / #00ccff / #66ff00 / #ff6600 / #cc00ff / #f6447f`，其余为白。所谓"保持原生不改动"就是直接用这张表，而不是自己挑颜色。
- **横条插在 `textWrap` 首个子节点位置**，并刻意避开放 `background-clip:text` 的 `.barrage-gradient` —— 落进去横条文字会被裁成透明。插入是幂等的（引擎节点池化会清空子节点）。
- **无空轨时 `addComment` 返回 false 并静默清理**（管理器那层的自适应丢弃在 addComment 路径上不生效），所以限流与丢弃计数是我自己做的：每秒最多 12 条，超出丢弃并计数。

### 聊天区的三条硬约束（都落实了）

1. **绝不带 `is-self`** —— `BarrageSendCheck` 以它为门槛比对回执；一旦命中，外房弹幕会被标删除线 +「(可能发送失败)」并污染熔断计数
2. **插入时若本来就在底部则同步滚底** —— 否则 `scrollHeight` 变大触发原生"离底 > 30px"判定，进锁屏态并开始显示"有 N 条新消息"
3. **上限只回收我自己插入的条目**，遇到原生条目立刻停手

### 顺带修掉一个既有缺陷

`RemoveRepeatedDanmaku` 用 `dom.textContent.trim()` 当去重键，会把勋章、右侧附加文本、以及新的来源横条统统算进去 —— 后果是跨房同文本弹幕无法去重，且 key 随附加内容漂移导致 map 膨胀。改为优先取引擎给的 `comment.data.text`，并带上来源房间前缀：不同房间的相同内容各自计数，同房间的重复才归并。没有 `comment` 时仍退回 `textContent`，老路径不受影响。

另外实测确认 **`ImageDanmaku` 当前并没有接线**（`initPkg()` 里没有它），所以"图片弹幕占位冲突"这个问题不存在，没有可修的东西。

---

## 五、验证情况（哪些验过、哪些要你实机确认）

**离线已验证**（`npm test` 全绿，共 6 个测试文件；本次新增两个测试文件、27 组断言）：

| 覆盖项 | 说明 |
|---|---|
| 四套布局的落点判定与死区边界 | 逐个百分比边界核对，含 4 分屏"无死区"、3 分屏 `0.664` 分界、5 分屏四边死区 |
| `is-multiN` 布局与 z 序 | 3/2/4/5 分屏的每格内联坐标与 z-index 逐一比对实测原值 |
| 拖拽换位后的一致性 | 数据 / 播放器记录 / 类名三者同步，并验证**起手格的子节点没有被 reparent** |
| 死区松手取消、短按不触发拖拽 | 含"长按定时器必须被 mouseup 取消" |
| 编辑条四项校验与文案 | 四条分支逐一核对文案，主房间静默返回 |
| 箭头步进 | `286 × floor(条宽/286)`，含条宽不足 286 的退化 |
| 关键几何 1:1 保真 | 房名胶囊 / 卡片 / 箭头 / 角标 / 重载按钮 / 信息条的实测原值逐条比对 |
| 弹幕补丁顺序无关性 | 两种顺序下 `raw.comment` 都先于钩子就位、补丁后仍是合法 JS、注入次数恒为 1 |
| 真实引擎源码级验证 | 把补丁作用到真实的 490KB `firstqueue` 源码上：锚点唯一、补丁后合法、钩子只注入一次 |
| 报文解析 | 真实 chatmsg 报文（含 `bnn` 干扰、`@S` 反转义、二进制头残字符）、非弹幕报文返回 null、原生色表逐档核对 |
| 聊天区结构与约束 | "绝无 `is-self`"、彩色弹幕带原生色值、白色不加内联色、上限只回收自有条目 |
| 引擎捕获与上屏载荷 | 上屏走原生 `addComment`、载荷形状与色值、无空轨计数、限流生效 |

测试过程中抓出并修掉了 **3 个真缺陷**，都不是靠肉眼看出来的：

1. **槽位按 1-based 查类名** —— 槽 1 对应的是 `is-multi2`，我一开始查 `is-multi1` → 真机上会直接判定"当前页面不支持多屏"，整个功能静默失效。
2. **`mousemove`/`mouseup` 只在拖拽期间挂载** —— 快速点一下（没进拖拽）时没有任何东西取消长按定时器，手已经松开了 300ms 后仍会凭空进入拖拽态并一直卡住。改为三个监听常驻绑定。
3. **收包头二进制残字符导致锚定匹配整体落空** —— 报文解析直接返回 null，加了一层"切到首个 `type@=` 再试"的兜底（仍不是 `startsWith`）。

### 实机验证（内置浏览器打开 2288 房间，把产物注入真实页面实跑）

**已实跑通过**：

| 项 | 实机结果 |
|---|---|
| 槽位结构 | `#js-player-multiContainer` 下正好 5 个槽位，类名身份 `MAIN / is-multi2 / is-multi3 / is-multi4 / is-multi5` —— 与我的槽位编号完全一致 |
| 三格布局 | 槽 0 = 533×300（65.6%）、槽 1/2 = 267×150（32.8%）在右侧上下，内联坐标与 z 序逐项对齐 `MS_LAYOUT[3]` |
| 四格布局 | 2×2 每格 407×229，坐标 @32,196 / @439,196 / @439,425 / @32,425 |
| **外房真的出画面** | 三个外房流全部 `readyState 4` 且 `currentTime` 持续增长（真在播），`<video>` 用 blob: 源，取流链路走通 5 次 `getH5PlayV1` |
| 房名胶囊 | 每格左下角正确显示对应主播名，`display:flex`、字号 14px（原生实测值） |
| 加载遮罩 | 出画面后正确收起为 `display:none`，不挡视频 |
| **N 路弹幕连接** | 为 3 个外房真的建起 3 路 WebSocket，随房间增删同步开关 |
| **聊天区合并** | 实机插入 100 条：来源横条正确截到 5 字（「金咕咕金咕」「南波儿大魔」）、正文原样、昵称正确，**全库 0 条 `is-self`**（那条硬约束在实机成立） |
| **真实鼠标交互** | 真实点击右上格 → 正确开出了该房间（`/252140`），证明事件确实穿过全屏透明覆盖层到达了处理函数 |

**实机验证又抓出 4 个真缺陷，已全部修复**（详见提交 `629195f`）：

1. **拖拽/点击整体失效**：播放器区域最上层是全屏透明但 `pointer-events:auto` 的 `#__h5player`（旧播放器树），它与多屏容器**同尺寸同位置却不包含容器**，两者分属两棵树。原生把事件挂在它上面正是因为它才是真正的事件面 —— 而我先挂它、后挂容器祖先，两种都收不到事件。改为四个监听全挂 `document`，命中一律按容器矩形坐标判定。
2. **格子会静默塌回一格**：斗鱼在播放器初始化/切房时会重建容器里的槽位节点，内联尺寸与格子内容一起丢失，而容器类名还留着 `is-multiN`。加 `MutationObserver` + 250ms 防抖自愈。
3. **编辑条打开时拖拽换位后不再刷新**：`MultiScreen_onChange` 是单槽回调，编辑条与弹幕合并都注册它，后注册的静默顶掉先注册的。改为数组多播。
4. **点一下格子开出两个标签页**：初始化不幂等，重复调用会重复注册监听（实机复现）。

**仍未实机跑到的**：

1. **弹幕飘屏（合并的那一半）**：脚本钩子必须在 `document-start` 生效，而"注入产物"这种取证方式跑不到那个时机，所以 `MergeDanmaku_getStats().ready` 在本次注入环境下始终是 `false`（聊天区那一半不依赖引擎，已实机通过）。你在 TM 里正常安装后是可用的 —— 进直播间看飘屏上是否带来源横条即可；也可以在控制台执行 `MergeDanmaku_getStats()` 看 `{ready, conns, pushed, dropped, noTrack}`。
2. **长按拖拽的真实手感**：CUA 的按下-移动间隔短于 300ms 长按阈值，真实拖拽没能起手（残留为 0 也印证了"定时器被 mouseup 正确取消"）。拖拽逻辑本身由测试场景 11/12/13/19/20 覆盖，但**手感**（长按多久、跟手是否顺、0.2s 回位是否自然）建议你实机摸一下。
3. **编辑条顶栏的实际高度**：画质胶囊已压在 24px 内以保住原生 226px 条体，想看一眼与标题、关闭按钮的对齐是否得体。
4. **搜索接口的返回结构**：形状是规划阶段实测记录的，代码做了多重兜底，本次没再跑一遍。
5. **斗鱼控制条上的入口**：原生在播放器控制条里有「开启多屏 / 退出多屏 / 编辑多屏」。我**没有**去改斗鱼自己的控制条（那是它的 React 子树，插进去要跟它的重渲染抢），入口改由本插件的 Dock 面板承担：点播放器图标 → 面板切到「多屏观看」→ 添加房间 → 开启多屏 / 编辑位置顺序。

**顺带发现的两处既有健壮性问题**（与本次改动无关，未改）：

- `common.js` 靠 `html.innerHTML` 里的 `$ROOM.room_id =` 解析房间号。这一版页面自身的 html 里**没有**这个标记，所以走的是 canonical 链接兜底（实机确认 `rid = 2288` 正确）。它本身没问题，但注意：任何会把包含该字面量的脚本塞进 DOM 的做法（比如我这次的注入取证）都会让它解析错。
- `initPkg_LiveTool_Treasure` 在缺少它假设的容器时会抛错，从而中断 `initPkg()` 后续的所有包（实机复现）。正常页面里容器存在，所以不影响线上；但"一个包抛错会连累后面所有包"这个结构性风险值得单独处理。

---

## 六、怎么用

1. 装/更新 `D:\DouyuEx-RL\DouyuEx_RL.user.js`
2. 进任意直播间 → 点左边 Dock 的「同屏播放」图标 → 面板顶部切到「多屏观看」
3. 用搜索框（房间号 / 直播间链接 / 主播名）或「关注在播」「最近看过」加房间，然后点「开启多屏」
4. 播放器区域会按加入的房间数自动分成 2/3/4/5 格
5. **长按任意一格 0.3 秒**再拖动，可换位；松在空白死区或原地不动都会取消
6. 「编辑位置顺序」打开原生那条编辑条：卡片可勾选增删、左右箭头翻页、顶栏可切画质档位
7. 外房弹幕会带着「来源横条 + 主播名」飘过，同时出现在右侧聊天区

**一个可一行切换的口径**：点小格的行为，原生是 `window.open("/"+rid)` 开新标签（多屏是辅助，点格去该房间主页）。我按"复刻"默认保留了这个行为。如果你更想要"点小格就把它切到主画面"，把 `PopupPlayer_MultiScreen.js` 里的 `MS_CLICK_OPENS_NEW_TAB` 改成 `false` 即可。

**「最近看过」说明**：斗鱼没有可用的公开"最近观看"接口，项目里也没有既有数据源。所以这份列表是本插件自己维护的本地记录（`ExSave_MultiScreenRecent`，上限 20 条）—— 凡进过多屏的房间都会记一笔。这是如实的设计选择，不是抓的斗鱼数据。

---

## 七、提交

| 提交 | 内容 |
|---|---|
| `c965f56` | feat(多屏)：1:1 复刻斗鱼原生多屏 —— 格子引擎 + 拖拽换位 + 编辑条 + 房间选择（新增 `tests/test_multiscreen.js` 18 组断言） |
| `1ba781c` | feat(多屏)：弹幕合并 —— 喂原生引擎保留全部颜色特效 + 来源横条 + 聊天区同步（新增 `tests/test_danmaku_merge.js` 7 组断言） |
| `629195f` | fix(多屏)：实机验证抓出的 4 个真缺陷 —— 事件宿主、自愈、多播回调、重复初始化（多屏测试扩到 20 组断言） |

文件拆分为改造 `PopupPlayer` 包（不新建包）：`PopupPlayer.js`（面板双页 + 房间来源）、`PopupPlayer_MultiScreen.js`（格子引擎 + 拖拽 + 持久化）、`PopupPlayer_EditBar.js`（编辑条）、`PopupPlayer_MergeDanmaku.js`（引擎注入 + 壳 + 聊天区）。样式统一落在 `ExPanel.css` 第 20~23 节（`ms-*` / `ex-ms-*` 命名空间）。

新增存储键：`ExSave_MultiScreen`（房间列表 + 画质档位）、`ExSave_MultiScreenRecent`（最近看过），均遵循 `ExSave_` 前缀。

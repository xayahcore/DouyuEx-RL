/* ==================== 版本更新日志数据源（SSOT 单一数据源） ==================== */
/*
 * 【维护约定】新增版本时只需在 EX_UPDATE_LOG 顶部追加一个以新版本号为键的条目，
 *            渲染层（Update.js）会自动查表并生成三大板块，无需改动任何渲染代码。
 *            日志内容只写「上一个版本 → 本版本」的变更，历史版本条目无需保留。
 *
 * 【结构约束】以下四条由 build.js 在构建期强校验，任一不满足将直接中断构建：
 *   1. 必须存在与 src/main.js 中 @version 完全一致的版本号键；
 *   2. 每个版本必须包含且仅包含 EX_UPDATE_LOG_SECTIONS 声明的三个板块；
 *   3. 三个板块都必须是非空数组，不允许留空占位；
 *   4. 每个条目必须为字符串，且以 "【分类】" 开头（渲染层统一加 "• " 前缀）。
 */

const EX_UPDATE_LOG_SECTIONS = ["新增功能", "改进与修复", "其它"];

const EX_UPDATE_LOG = {
  "2026.09.22.05": {
    "新增功能": [
      "【历史弹幕】新增用户弹幕历史查询：在斗鱼用户卡片上点「弹幕历史」，直接在本页弹出的磨砂面板里查看该用户的历史弹幕与礼物记录，支持翻页、按天回溯、一键跳转到对应直播间。此前同类功能只能跳转到第三方网站，现改为页内直接查看，不用离开直播间。",
      "【历史弹幕】卡片按钮自动注入：斗鱼每次点击用户名都会重建卡片节点，因此改用去抖的 MutationObserver 探测（弹幕列表每秒突变多次，去抖后整页扫描最多每秒 5 次），按钮复用斗鱼原生卡片按钮样式，与「举报用户」「屏蔽用户」「发站内信」「关注」并排显示，视觉零违和、不引入任何自定义样式。",
      "【历史弹幕】零接口获取用户 ID：直接从卡片链接的 href 解析 uid，不再依赖昵称反查（原方式要求昵称精确匹配，用户改过昵称就永远查不到）。"
    ],
    "改进与修复": [
      "【历史弹幕】修复至尊用户与礼物展馆用户的卡片没有入口：斗鱼按用户等级渲染多种卡片组件（实测 NormalCard 普通 / SupremeCard 至尊 / GiftHallCard 礼物展馆），三者内部结构一致但类名前缀完全不同，此前只匹配 .NormalCard-action，导致其余两种卡片上根本不出按钮。现改为按后缀匹配全部卡片变体，并让按钮类名逐字克隆同排原生按钮（含 is-supreme10 / is-noble0 等等级修饰类），三种卡片下样式均与原生完全一致。",
      "【历史弹幕】修复弹窗挡住聊天区导致无法继续查下一个用户：弹窗会覆盖弹幕列表所在的区域，未收起时用户去点别的用户名会打在弹窗上，表现就像是「换了个人但没重新查询」。现改为点击弹窗外部自动收起。",
      "【历史弹幕】用户识别不再依赖闭包捕获：斗鱼会复用同一张卡片节点展示不同用户（实机测得同一节点被反复复用），卡片引用改为在点击那一刻现取并重新解析 uid，避免停在旧对象上。",
      "【弹幕查询】修复既有跳转目标失效：原「查弹幕」按钮跳转的地址带 dt=0 参数，该模式在数据源端已返回 502 Bad Gateway，按钮实际早已不可用；现改为查询当天直播日。",
      "【历史弹幕】未登录状态如实提示：数据源须登录才能读取，遇到跳转登录页时明确提示「需先登录 doseeing 才能查询历史弹幕」并提供一键前往登录，不假装查询成功。",
      "【历史弹幕】请求三条路径必定回调：成功、失败、超时全部复位加载状态并给出可重试提示，避免网络异常时界面永远停在「加载中」；快速切换日期时丢弃过期响应，防止跨日期数据串台。"
    ],
    "其它": [
      "【测试纪律】新增历史弹幕专项测试：覆盖卡片 uid 解析、三种卡片变体（普通/至尊/礼物展馆）入口注入与按钮类名克隆、点击外部收起、未登录判定与兜底、分页边界、弹幕与礼物两类行渲染、错误与超时状态复位、本地日期跨时区正确性。",
      "【历史弹幕】查询结果不做本地持久化：只在内存内做短期缓存以省去重复请求，不写入 localStorage，不新增存储键。"
    ]
  }
};

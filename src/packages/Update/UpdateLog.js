/* ==================== 版本更新日志数据源（SSOT 单一数据源） ==================== */
/*
 * 【维护约定】新增版本时只需在 EX_UPDATE_LOG 顶部追加一个以新版本号为键的条目，
 *            渲染层（Update.js）会自动查表并生成三大板块，无需改动任何渲染代码。
 *            日志内容只写「上一个版本 → 本版本」的变更，历史版本条目无需保留。
 *
 * 【结构约束】以下四条由 build.js 在构建期强校验，任一不满足将直接中断构建：
 *   1. 必须存在与 src/main.js 中 @version 完全一致的版本号键；
 *   2. 每个版本必须包含且仅包含 EX_UPDATE_LOG_SECTIONS 声明的三个板块；
 *   3. 三个板块都必须是非空数组，不允许留空占位；
 *   4. 每个条目必须为字符串，且以 "【分类】" 开头（渲染层统一加 "• " 前缀）。
 */

const EX_UPDATE_LOG_SECTIONS = ["新增功能", "改进与修复", "其它"];

const EX_UPDATE_LOG = {
  "2026.09.26.02": {
    "新增功能": [
      "【签到】一键签到新增「活动中心签到领积分」：自动完成活动中心每日签到，并把签到后可领的礼包逐个领掉。活动别名与奖励档位每次实际读取、不写死在脚本里，活动换名或档位调整都不会让它失效。",
      "【签到】一键签到新增「看直播领积分（自动）」：观看时长达标后自动领取对应档位（当前活动为 300 / 900 / 1200 / 1800 秒各 10 积分，3000 秒 20 积分）。只在页面可见且视频播放中累计时长，切到后台或暂停都不会白算；每跨过一档只上报一次，全部领完后彻底停止请求。默认关闭，勾选立即生效，取消即完全停止。"
    ],
    "改进与修复": [
      "【视频工具】修复鼠标划过播放器时摄像头画面不再弹出：某次全量重制漏掉了「已手动关闭」这个状态变量的声明，导致划过播放器、划过摄像头区域这几处事件一触发就抛 ReferenceError 并中断，摄像头叠加画面因此形同失效；现已补回该声明。"
    ],
    "其它": [
      "【测试纪律】新增「签到 / 看播领积分」专项测试 17 项，把请求方式写反会静默失败、未登录如实报错、活动已过期提前止损、看播只统计前台播放时长、跨门槛只上报一次、失败重试上限与冷却、全部领完后彻底停发请求等口径全部钉住。"
    ]
  },
  "2026.09.26.01": {
    "新增功能": [
      "【界面】左下角通知全面换装：由旧版浅色改为 MIUIX 深色磨砂玻璃，与插件面板视觉统一；成功 / 信息 / 警告 / 错误四种类型各有独立图标，进度条由粗条改为 2px 细线。"
    ],
    "改进与修复": [
      "【通知】停留时长由 3 秒改为 4 秒，并把它显式声明为常量，不再依赖通知库的默认值。",
      "【界面】修复弹幕投票抽屉会自己弹开：五个抽屉的收起态此前只有四个写进了统一引擎，投票抽屉漏了这条规则，进房后它会自动展开；现已收归同一处。",
      "【真实人数】修复刷新页面后统计条消失：统计条此前可能被插进会被持续重绘的弹幕区、随后被冲掉，而且插错之后不会再恢复；现在固定插在播放器区块，被冲掉会自动重建，节点不在时也不会每 5 秒抛一次异常。",
      "【真实人数】修复靓号房间的人数统计全是 0：这类房间地址栏显示的是展示短号（例如 /15000），此前脚本把它当成了真实房间号，而数据源只认真实房间号（用短号会返回 200 但各项全 0）。现在会在页面就绪后解析出真实房间号并自动纠偏 —— 背包、禁言、宝箱、活跃榜、鱼吧等同样依赖房间号的功能也一并恢复。",
      "【真实人数】统计条重建后立即回填数据：此前节点被页面重绘冲掉、重建出来之后，各项数字要等下一次刷新周期（最长 150 秒）才回来，期间一直显示 ****。",
      "【真实人数】数据源不可达时不再连累本地项：「已播」时长只依赖本地开播时间，此前数据源一失败整条统计都不更新，现在数据源异常时本地项照常显示，也不会把已经取到的人数覆盖成 0。"
    ],
    "其它": [
      "【构建纪律】构建期新增 CSS 非法八进制转义检查，并把报错定位精确到具体文件（此前只给整体位置，而 CSS 在产物里被压成一行后无法定位）。",
      "【测试纪律】测试依赖 jsdom 纳入 devDependencies，缺失时明确提示先执行 npm install；并去掉测试里写死的本机绝对路径（此前换台机器全新 clone 后第一个测试就会崩）。",
      "【测试纪律】新增「真实人数统计条」专项测试 10 项：覆盖靓号房间号解析、统计条落点、冷启动补插、被重绘冲掉后的自愈与去重、节点缺失不抛异常、数据源异常时的取值行为。"
    ]
  }
};
